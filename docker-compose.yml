version: '3.8'

services:
  rideviz-rs:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_POSTHOG_API_KEY=${VITE_POSTHOG_API_KEY}
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
