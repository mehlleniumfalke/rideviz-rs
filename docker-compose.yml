version: '3.8'

services:
  rideviz-rs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
      - PORT=3000
      - MAX_FILE_SIZE_MB=25
      - CACHE_TTL_SECONDS=3600
    restart: unless-stopped
